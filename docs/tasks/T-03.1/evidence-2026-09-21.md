# E-01a — Native baseline execution record

Run date: 2026-09-21 (America/New_York); observations span 21–22 September UTC. Task: T-03.1 / R-011. First execution preflight reading: 21 September, 22:50:28 UTC. **Result: passed for the declared native baseline and recovery boundary.** All three action/reload cycles matched; B survived a restart; restored A loaded successfully; original Multiplayer/Cloud preferences and a final synchronized A load were verified. Neither the user's originally running game nor the setup launch is counted among the three cycles.

[STEPS](../../STEPS.md) owns delivery status. The [task plan and E-01a procedure](README.md) own preparation and acceptance; the [source review](sources.md) holds publisher/vendor findings.

## Claim, scope, and observers

The claim to test is that one identified native installation can repeat the selected ship interaction and save/reload behavior, then restore a declared disposable checkpoint. One local game client is involved. The user operates the interactive checkpoints; Codex records filesystem/configuration checks and coordinates the procedure. User observations and tool observations are identified separately.

The user authorized this step's execution after the documentation relocation. No adapter, server, mod package, save editor, multiplayer experiment, or entitlement API is used. The user reports that the Steam copy is owned, that no existing saves need preservation, and that the account is used only on this PC. Existing save files remained outside the replacement set for the new disposable slot.

## Measured preflight

| Item | Observation | Method / limit |
| --- | --- | --- |
| Running game | `NMS` process present; user reported main menu/save selection | Process query; no baseline interaction observed yet |
| Storefront | Steam AppID `275850`, installed build `25442159`; public branch, updated 21 September 2026 | Selected appmanifest fields plus user's direct Steam Properties observation |
| Executable | `D:\SteamLibrary\steamapps\common\No Man's Sky\Binaries\NMS.exe` | Running process path |
| SHA-256 | `B7913F268DFC62386B6B68F524BFC8ADE4A44A9F4FBAD39085B7BF51BE3680CB` | Execution preflight hash matches preparation |
| System | Windows 10 Pro 64-bit build 19045; i9-12900K; 95.77 GiB RAM | Preparation hardware record; no hardware change reported |
| Configured renderer/display | RTX 3080; borderless 1920 x 1080; HDR off; triple-buffer VSync; 60 FPS cap | `TKGRAPHICSSETTINGS.MXML`; configured values are not measured performance |
| Graphics | FFXSR2 anti-aliasing, Balanced FFXSR2 quality; inspected texture, animation, shadow, reflection, volumetric, planet, water and base detail values are High | Selected nested graphics properties |
| Input | Ordinary monitor, keyboard and mouse | User report |
| Network conditions | One local client; lab Multiplayer and per-game Steam Cloud off during the counted cycles and file recovery; original settings restored for the final check | No network fault injection or packet capture; connection type, latency and multiplayer behavior were not measured |
| Native multiplayer preference | `Multiplayer=true` | `GCUSERSETTINGSDATA.MXML`; record any change and restore the original preference |
| NMS Cross-Save preferences | `CrossSaves=false`, `CrossSavesAutoUploads=true` | Stored game settings; these two fields must not be conflated, and account/UI behavior is not inferred from the auto-upload field |
| Steam Cloud | Originally on; temporarily off for this lab | User observed the original setting, then confirmed normal game exit, completed synchronization, and the per-game switch off before the original backup |
| Mod baseline | Neither inspected `GAMEDATA\MODS` nor `GAMEDATA\PCBANKS\MODS` exists; `DisableAllMods=false` | Targeted folder/settings inspection; does not certify the executable against a publisher reference or exhaust every possible modification path |
| Save location | `$APPDATA\HelloGames\NMS\<steam-profile>` | One profile observed; private account path remains in ignored local storage |
| Existing save-related files | `save.hg`, `save2.hg`, `mf_save.hg`, `mf_save2.hg`, dated February 2025 | Metadata only; initial hashes retained for comparison; no contents imported or modified |
| Other native data | Account data, Steam cloud metadata, and a season cache are present | Outside the proposed disposable-save replacement set; account identifiers and data are not included here |

## Local artifacts and commands

Private execution directory: `local/t03-1/run-20260921-225259/`. Capture directory: `artifacts/t03-1/run-20260921-225259/`. Both are ignored. The first game-only capture is `setup-20260921-233120.png`; it shows the Cosmos update information screen at 1920 x 1080, with UI version `179666 (Save Version 4226)`. This is a tool-observed menu state, not a loaded-save result.

`preflight-private.json` contains the actual path mapping, executable hash, four existing save-related file hashes, and four initial game-settings file hashes. It is a metadata manifest, not a save backup. Original command sources are retained as `preflight-inspection.ps1` and `record-preflight.ps1`; the latter command's output is in `preflight-record-output.txt`.

`backup-original.ps1` completed at 23:29:37 UTC after the user's closed-game/sync/Cloud-off confirmation. It checked that NMS was absent before and after copying, confined destinations to this run's new snapshot directory, refused an existing snapshot, and verified source/copy hashes. Exact invocation from the repository root:

```powershell
& .\local\t03-1\run-20260921-225259\backup-original.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259'
```

Exit code 0: all 12 files copied into `original-snapshot/` passed SHA-256 verification (eight profile files and four settings files). All four pre-existing save-related files still matched their preflight hashes. `original-snapshot-private.json` records exact private paths, sizes, hashes, and comparison results; `original-snapshot-output.txt` retains the command output. No source file was replaced. This original recovery copy was retained. The actual restoration used the later native-verified disposable checkpoint A described below.

`setup-launch.ps1` launched the game through `steam.exe -applaunch 275850`; `setup-launch.json` records request time 23:30:19.871 UTC, process start 23:30:21.086 UTC, and the unchanged executable hash. This was the disposable-save setup launch, not a measured cycle. `capture-game.ps1` captured only the foreground NMS client area at 23:31:20.657 UTC without changing focus. Those timestamps do not measure menu or world load duration.

At the next user checkpoint, the user confirmed the new Creative save had loaded near the ship, then they disabled Multiplayer, walked next to the ship, and opened the Esc menu. A 23:54:02.778 UTC XML property check corroborated `Multiplayer=false`; `CrossSaves=false` and `CrossSavesAutoUploads=true` remained unchanged. These observations are retained in `setup-checkpoint-observation.json`. The game was not foreground during the capture attempt, so no additional screen was captured and the loaded scene is user-reported. No action or load duration was timed during setup.

After the subsequent instruction to proceed, `Get-Process NMS` found no game process. The closed-game comparison and candidate backup ran successfully with these commands:

```powershell
& .\local\t03-1\run-20260921-225259\inspect-closed-profile.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'after-setup'
& .\local\t03-1\run-20260921-225259\backup-disposable.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -SnapshotName 'checkpoint-a-candidate' -ReferenceInventory 'inventory-after-setup-private.json' -SaveFiles @('save3.hg','save4.hg','mf_save3.hg','mf_save4.hg')
```

Both commands exited 0. `inventory-after-setup-private.json` records 18 files: four new save-related files (`save3.hg`, `save4.hg`, `mf_save3.hg`, `mf_save4.hg`), two new intro-feed cache files, and the twelve originally observed files. The shared `accountdata.hg` / `mf_accountdata.hg` and `GCUSERSETTINGSDATA.MXML` changed during native setup. No original file was missing; all four old save-related files remained hash-identical. The executable hash still matched preflight, Multiplayer was off, and the two Cross-Save preferences were unchanged.

`checkpoint-a-candidate/` contains verified copies of the four new save-related files and four settings files; all eight source/copy hash checks passed. `checkpoint-a-candidate-private.json` records exact source/destination paths and hashes. The four new save-related files are the replacement allowlist. The settings copies are reference/recovery data; shared account data, cache files, and older saves are outside that allowlist. The user subsequently confirmed naming the save `T-03.1 checkpoint A`, entering/exiting the landed ship, and seeing save confirmation. `checkpoint-a-user-confirmation.json` records that reply; the exact setup save message and controls were not reported. The snapshot keeps its original directory name. A's later native name/location reload is confirmed below; no game files had been replaced at this checkpoint.

Cycle 1's initial launch used:

```powershell
& .\local\t03-1\run-20260921-225259\launch-baseline.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'cycle-1-start'
```

`launch-cycle-1-start.json` records NMS absent before launch; request at **22 September, 00:03:34.171 UTC**, process `25920` started at 00:03:35.580 UTC, and request-to-process-start **1.409 seconds**. The Steam build remained `25442159`, the executable hash matched, and all four settings hashes were recorded at launch. This timing excludes menu readiness and save loading. The user was instructed to load the named Creative slot normally from the save list, estimate selection-to-on-foot load time, enter/exit the landed ship, report the controls/save feedback and any errors or stutter, and quit normally. The following restarts and native reload observations are recorded separately below.

The first launcher console summary incorrectly displayed null fields because of PowerShell dictionary projection; `launch-cycle-1-initial-console.txt` retains that output. The persisted JSON was correct and was read back alongside the actual process query. Console formatting was corrected without launching another process; this was an observation-tool formatting issue, not an observed game failure.

After cycle 1's action/exit instructions, the user reported that everything felt normal and fast, with a black screen during an unspecified exit transition. They subsequently reported normal return and estimated the brief transition at under one second. This is a subjective estimate; the precise transition, duration, and cause are unverified. Save loading was described as fast, without a reliable numerical duration. The exact controls and save-message wording were not supplied. These observations and their limits are retained in `cycle-1-action-observation.json`; no game defect or measured performance change is inferred from them.

The game process was absent after that report. The following closed-game inventory and restart both exited 0:

```powershell
& .\local\t03-1\run-20260921-225259\inspect-closed-profile.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'cycle-1-exit'
& .\local\t03-1\run-20260921-225259\launch-baseline.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'cycle-1-reload-cycle-2-start'
```

`inventory-cycle-1-exit-private.json` found the same 18 files and unchanged old saves, with the original executable hash and lab preferences intact. All four settings files matched checkpoint A byte-for-byte. Of the four disposable files, `save4.hg` / `mf_save4.hg` changed since A, while `save3.hg` / `mf_save3.hg` remained identical; this describes files, without inferring their save-type semantics.

`launch-cycle-1-reload-cycle-2-start.json` records a new launch request at **22 September, 00:08:39.956 UTC**, process `41816` starting at 00:08:41.621 UTC, and **1.665 seconds** from request to process start. The prior process was absent and the build still matched. The user was asked to confirm A's name/location after loading, completing cycle 1's restart check. That launch/load also began cycle 2's separate ship action and normal exit; each action and its following restart were counted separately.

The next user report supplied the exact ship-exit feedback, **“Restore point saved,”** and said the planet name appeared inside the ship rather than in that popup. The location check concerns returning beside the same ship in the same place, not a planet name in the notification. The user subsequently confirmed that A's save name and location matched after the restart; `cycle-1-reload-confirmation.json` records this and completes cycle 1. NMS was observed absent at **22 September, 00:11:02.358 UTC**. `inspect-closed-profile.ps1`, with the same run directory and `-Phase 'cycle-2-exit'`, exited 0 and wrote `inventory-cycle-2-exit-private.json`. The same 18 files remained present, all four old saves were unchanged, the build matched, and all four settings files still matched checkpoint A. `cycle-2-action-observation.json` retains the user report and comparisons.

The next `launch-baseline.ps1` invocation used the same run directory and `-Phase 'cycle-2-reload-cycle-3-start'` and exited 0. Its launch manifest records request at **22 September, 00:13:22.400 UTC**, process `8692` starting at 00:13:24.016 UTC, and **1.616 seconds** to process start, with NMS previously absent and the original build/hash retained. The user was asked to check A's name/location again, perform the third ship action, note the enter/exit key and save message, and quit normally. That name/location observation completed cycle 2; cycle 3 had its own subsequent restart.

The user answered “Yes same” to that matching-name/location and third-action check. NMS was closed; `inspect-closed-profile.ps1 -RunDirectory <the recorded run directory> -Phase 'cycle-3-exit'` exited 0, finding the same 18 files, unchanged older saves, the same executable, and all four settings hashes matching A. The exact enter/exit key was not supplied. Cycle 2's reload check was complete.

`launch-baseline.ps1` then used `-Phase 'cycle-3-reload-b-setup'`: request **22 September, 00:15:24.832 UTC**, process `28552` start 00:15:26.221 UTC, **1.390 seconds** to process start, with the original build/hash and a confirmed prior process exit. The user was instructed to load A, confirm its name/location, then rename the same slot B, enter/exit the ship and quit. Their reply reported everything the same, completing cycle 3's name/location reload check. The three action/reload cycles are therefore complete; control-key details and numerical world-load times remain unmeasured. `cycles-2-3-and-b-setup-observations.json` preserves these replies and limits.

The following commands then exited 0, using the previously recorded run directory:

```powershell
& .\local\t03-1\run-20260921-225259\inspect-closed-profile.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'b-created'
& .\local\t03-1\run-20260921-225259\backup-disposable.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -SnapshotName 'checkpoint-b-candidate' -ReferenceInventory 'inventory-b-created-private.json' -SaveFiles @('save3.hg','save4.hg','mf_save3.hg','mf_save4.hg')
& .\local\t03-1\run-20260921-225259\launch-baseline.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Phase 'b-verify'
```

The B-candidate snapshot passed all eight copy/hash checks; older saves remained unchanged and the executable still matched. `launch-b-verify.json` records request **22 September, 00:18:41.722 UTC**, process `19044` start 00:18:43.256 UTC, **1.534 seconds** to process start. The user was instructed to see `T-03.1 checkpoint B` in the save list, load it normally, confirm B remains the name in Options, and quit without renaming. They reported **“It still says A.”** This attempt did not establish a distinguishable B state. They subsequently clarified that they did rename A to b once before finding A again; the full prior label was not independently captured. `b-verification-attempt-1-observation.json` and `b-rename-clarification-and-save-times.json` retain the reports. No restoration had been attempted at that point, and the candidate B copy was excluded as verified B evidence.

Read-only metadata at **22 September, 00:29:23.613 UTC** showed `save3.hg` / `mf_save3.hg` still last written at **21 September, 19:58:38 local**, while `save4.hg` / `mf_save4.hg` were last written at **20:16:58 local**. A different running process (`41208`) was observed; `b-investigation-process.json` records its start time and executable hash. This additional investigation launch is outside the three counted cycles. The publisher's [Waypoint saving notes](https://www.nomanssky.com/waypoint-update/), rechecked on 2026-09-21 local time, describe continuing autosaves and ship-triggered full saves separately. **Hypothesis:** different saved versions may explain the displayed A label; filenames/timestamps alone do not establish which native reload operation uses which pair. The user was asked to read the current name and the Autosave/Restore Point times in Options without renaming, entering/exiting the ship, or selecting a reload. The native UI observations follow below.

The user supplied a screenshot at `C:\Users\admin\Downloads\Capture.PNG`. Visual inspection showed **`Rename Save (T-03.1 checkpoint A)`**, **`Reload Restore Point [9/21/2026 (20:16)]`**, and **`Reload Autosave [9/21/2026 (19:58)]`**. These native times correlate with the two file-pair timestamps above. The user was instructed to select only the 20:16 Restore Point, then read the name in Options without renaming or making another ship save. `save-menu-before-restorepoint-observation.json` records the inspected fields. The screenshot disappeared from Downloads before an optional archival copy; the image is present in the conversation, but no local raw copy or image hash is claimed.

That Restore Point also showed A, and the user expressed uncertainty about the earlier save sequence. The saved-version hypothesis therefore did not establish a B state. After the user's confirmed normal exit, `inspect-closed-profile.ps1` used `-Phase 'a-refresh-after-native-reload'`; `backup-disposable.ps1` used `-SnapshotName 'checkpoint-a-verified'`, its resulting inventory, and the same four-file allowlist. Both exited 0. All eight save/settings-reference copies passed their hashes; old saves and the executable remained unchanged. **`checkpoint-a-verified/` is now the A restoration source**, tied to the A state just confirmed after the native 20:16 Restore Point reload. Earlier snapshots are preserved. `checkpoint-a-verified-observation.json` records the reason for this fresh reference, and the restoration helper now points to its manifest.

The user requested better direct observation. `observe-nms.ps1` was added as an ignored local helper and started by `start-observer.ps1` in a hidden PowerShell **7.6.6** process at **22 September, 00:41:23.852 UTC**. It takes an unmodified screenshot of the foreground NMS client area every 1.5 seconds, retains twenty rotating frames in `artifacts/t03-1/run-20260921-225259/observer-20260922-004123/`, skips other foreground apps/minimized game windows, never changes focus or sends input, and stops after ten minutes or a `STOP` file in that observer directory. `observer-current.json` records the worker PID, script hash, settings and stop path. This observer was introduced for the recovery investigation after the three original cycles; those earlier cycles did not use continuous capture. No performance comparison is inferred.

`launch-baseline.ps1 -RunDirectory <the recorded run directory> -Phase 'observed-b-setup'` exited 0: request **00:41:24.424 UTC**, process `40056` start 00:41:25.901 UTC, **1.477 seconds** to process start, with the original build/hash. The user loaded A and showed Options. The observer captured a 1920 x 1080 frame at **00:42:55.029 UTC**, archived and hash-verified by `keep-observer-frame.ps1` as `artifacts/t03-1/run-20260921-225259/observed-ui/frame-20260922-004255-456.png`. Visual inspection confirmed A, Restore Point 20:16, Autosave 19:58, and the native Rename Save control. The user was then asked to open the rename box without typing, so subsequent instructions can use its actual confirmation controls. Raw frames may contain the player's display name and remain outside Git.

The captured rename dialog showed the existing A text and an explicit left-mouse **Accept** control beneath the field (`observed-ui/frame-20260922-004444-613.png`, captured 00:44:02.485 UTC). After the instruction to change the final letter and click Accept, a frame captured **00:45:33.935 UTC** showed **`Rename Save (T-03.1 checkpoint B)`** while the Restore Point remained **20:16** (`observed-ui/frame-20260922-004610-737.png`). This establishes accepted in-memory naming, not a new saved B checkpoint. `rename-dialog-observation.json` and `rename-b-accepted-observation.json` preserve the observations. The user was then asked to enter/exit the ship and show Options again for the save timestamp and name. File restoration had not yet run.

After the user reported exiting the ship and reopening Options, the observer's **00:49:48.611 UTC** frame showed **B**, **Restore Point 20:47**, and **Autosave 20:47** (`observed-ui/frame-20260922-005109-714.png`). The timestamps had advanced from the accepted-rename screen; the ship-save action is user-reported and the Options fields are directly observed. `b-observed-ship-save.json` retains this distinction. The user then confirmed a normal exit. `inspect-closed-profile.ps1` with `-Phase 'b-observed-saved-exit'` exited 0: eighteen files present, the four older save files unchanged, original executable hash, and unchanged lab Multiplayer/Cross-Save values.

The first observer reached its ten-minute limit at **00:51:25.700 UTC**, with 37 frames and no reported error. Inspection of its retained frames also directly showed on-foot Creative gameplay on Tatzesig (`frame-07.png`), the **E** interaction prompt for **Radiant Pillar BC1** (`frame-08.png`), the landed cockpit (`frame-10.png`), and the on-foot **“Restore point saved”** popup (`frame-13.png`). These files are in `observer-20260922-004123/`; `b-ship-action-frames-observation.json` retains their hashes and file-write times around 00:47 UTC. The exit-key prompt was not captured. This strengthens the B-setup action evidence without changing the observer limits of the three earlier cycles.

After confirming that worker stopped, `start-observer.ps1` renewed the same capture settings in `observer-20260922-005207/` at **00:52:07.843 UTC**. `launch-baseline.ps1` with `-Phase 'b-observed-verify'` exited 0: request **00:52:08.367 UTC**, process `38684` started **00:52:09.968 UTC**, **1.600 seconds** to process start, with NMS previously absent and the original build/hash. This launch tested B through the same ordinary slot load before any file restoration.

The user loaded the same disposable slot from the native save list and opened Options. The **00:54:45.977 UTC** frame from the new process showed **`Rename Save (T-03.1 checkpoint B)`**, with both save times still **20:47** (`observed-ui/frame-20260922-005457-438.png`). **B therefore survived a full process restart and ordinary slot load.** No in-session Autosave/Restore Point reload was needed. This ordinary slot-selection operation is the declared load operation for the forthcoming A recovery check. `b-observed-reload-verification.json` records the user report, screenshot hash and launch reference. The successful retest used explicitly observed Accept, ship save, normal exit and reload; the exact cause of the earlier uncertain sequence remains unproven.

The user confirmed another normal exit. `inspect-closed-profile.ps1` with `-Phase 'b-verified-before-restore'` exited 0, preserving the same build, lab preferences and older-save hashes. `backup-disposable.ps1` with `-SnapshotName 'checkpoint-b-before-restore'`, `-ReferenceInventory 'inventory-b-verified-before-restore-private.json'` and the same four-file allowlist exited 0: all eight save/settings-reference copies passed their hashes. This B snapshot is tied to the directly observed B reload; earlier candidate snapshots remain separate. `recovery-preconditions.json` records the actual B verification, the standing user-confirmed Cloud-off setting, the ordinary slot-load operation and the closed-game inventory.

`restore-disposable-a.ps1` checks the exact four-file replacement boundary and hashes, validates path ancestors, records attempted/partial writes, and verifies older saves again after replacement. An independent read-only review found three issues in its draft; partial-write logging, ancestor-path checks, and post-operation older-save checks were corrected and reviewed again without further concrete findings. Syntax checks passed before the recovery attempt. The actual restoration commands were:

```powershell
& .\local\t03-1\run-20260921-225259\restore-disposable-a.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259'
& .\local\t03-1\run-20260921-225259\restore-disposable-a.ps1 -RunDirectory 'C:\Users\admin\Documents\GitHub\nms-community-platform\local\t03-1\run-20260921-225259' -Apply
```

Both exited 0. The first validated without replacing files. The applied attempt at **00:59:06.301 UTC** restored only `save3.hg`, `save4.hg`, `mf_save3.hg`, and `mf_save4.hg` from `checkpoint-a-verified/`. **All four final hashes matched A**, all four older-save hashes remained unchanged, the build/lab settings matched, and NMS was absent during the operation. `restore-a-attempt-private.json` records exact source/target paths, planned and completed writes, final hashes and preservation checks. Shared account data, caches, settings and older saves were not replaced.

`launch-baseline.ps1` with `-Phase 'restored-a-verify'` then exited 0: request **00:59:31.288 UTC**, process `8156` start **00:59:32.643 UTC**, **1.355 seconds** to process start, with NMS previously absent and the original build/hash. The native check used the same ordinary slot-selection operation that verified B, followed by the name in Options and the user's ship/location comparison.

The **01:00:22.472 UTC** frame from the restored-A launch showed **`Rename Save (T-03.1 checkpoint A)`**, **Restore Point 20:16**, and **Autosave 19:58** (`observed-ui/frame-20260922-010055-283.png`). The user confirmed the same ship/location after the ordinary slot load. **The declared local B-to-A recovery passed**: B was independently reloaded, A's four files were restored and hash-checked, then the same slot-load operation showed A in place of B. `a-restored-native-verification.json` retains the observation and linked artifacts. Multiplayer and Steam Cloud recovery were checked separately below.

The second observer was stopped through its `STOP` file at **01:01:29.204 UTC**, with 33 frames and no reported error. A new ten-minute observer began at **01:01:29.321 UTC** in `observer-20260922-010129/` for the remaining native-settings and final-load checks, using the same game-only capture settings. The user restored Multiplayer through Network. The **01:02:00.915 UTC** frame directly showed **Multiplayer: Enabled**, matching the original preference (`observed-ui/frame-20260922-010259-377.png`); `multiplayer-restored-ui-observation.json` records the screenshot and user report. Post-exit persistence and Steam Cloud recovery were checked next; no other native setting was requested.

The user confirmed normal exit with Cloud still off. `inspect-closed-profile.ps1` with `-Phase 'preferences-restored-before-cloud'` exited 0: **Multiplayer=true**, **CrossSaves=false**, **CrossSavesAutoUploads=true**, unchanged older saves and the original executable hash. The mod, game and graphics settings files matched the original snapshot byte-for-byte. `GCUSERSETTINGSDATA.MXML` differed; `compare-restored-preferences.ps1` exited 0 and its line comparison found only one added **UnlockedTitles** entry (`T_MECH`, index 2), retained in `original-vs-restored-preferences-private.json`. No original setting line was removed or changed. This native title progression is outside the deliberately toggled Multiplayer/Cloud preferences and was retained. Disposable save hashes also changed during the normal loaded-game session; exact recovery hashes were verified before launching, not asserted across subsequent native writes.

The user then restored per-game Steam Cloud to **on** and reported **Up to date**, with NMS closed and no conflict reported. `steam-cloud-restored-user-observation.json` records the source of that setting/status evidence. `launch-baseline.ps1` with `-Phase 'final-cloud-on-verify'` exited 0: request **01:06:48.676 UTC**, process `31424` start **01:06:50.326 UTC**, **1.650 seconds** to process start, with NMS previously absent and the original Steam build/hash. The final ordinary slot load, post-exit synchronization and fingerprint/settings checks follow.

The final-load **01:08:05.971 UTC** frame showed **checkpoint A**, **Restore Point 20:16**, and **Autosave 19:58** (`observed-ui/frame-20260922-010833-328.png`). The user again confirmed the same ship/location. `final-cloud-on-a-load-observation.json` records this successful ordinary load with Cloud restored on. The user then confirmed normal exit and Steam Cloud **Up to date**.

After the final exit/sync confirmation, `inspect-closed-profile.ps1` with `-Phase 'final-cloud-on-exit'` exited 0: the same eighteen files were inventoried and all four older saves retained their original hashes. `final-closed-checks.ps1` exited 0 at **01:11:58.494 UTC**, recording **all checks passed** in `final-closed-checks.json`: NMS closed; Steam build **25442159**; executable file/product version **179666**, **88,480,328 bytes**, and the original SHA-256; original Multiplayer/Cross-Save values; unchanged mod/game/graphics settings; only the previously observed native title entry added to user settings; and both inspected mod directories absent. Steam Cloud's on/Up to date state is user-observed evidence. This is the final fingerprint and preference comparison, after the synchronized A load.

The last observer ended at **01:11:30.902 UTC** on its ten-minute bound, with 28 frames and no reported error. `observer-final-state.json` confirms it was stopped and its worker PID absent at **01:12:36.003 UTC**. The original recovery copy, native-verified A, verified B, earlier failed candidates, captures and local command sources remain in ignored storage. No proprietary files, account identifiers or raw captures are included in this report.

Preflight used `Get-Process -Name NMS`, `Get-FileHash -Algorithm SHA256`, selected-field `rg` reads of the Steam appmanifest, `Get-ChildItem` metadata, and XML property selection from `GCMODSETTINGS.MXML`, `GCUSERSETTINGSDATA.MXML`, and `TKGRAPHICSSETTINGS.MXML`. Only relevant property values and redacted path summaries were displayed. Commands and their outputs are retained in the local execution record; no application dependency was installed.

## Interactive procedure and restoration record

| Checkpoint | Observed result |
| --- | --- |
| Steam Cloud and branch confirmed | Original Cloud on/public branch observed by user; user subsequently confirmed NMS closed, sync complete, and per-game Cloud off |
| Original recovery snapshot | Complete: 12 verified copies; four existing save-related files unchanged; no source replacement |
| Disposable slot and exact file set identified | New Creative save labelled `T-03.1 checkpoint A` per user; four new save-related files identified; older saves unchanged. User confirmed native reload with the same name/location |
| Checkpoint A saved, game closed, backup verified | NMS observed closed and eight copies verified; user confirmed label, ship entry/exit, save feedback, and later name/location reload. Exact setup feedback/exit method not independently observed |
| Cycle 1: action, full exit/relaunch, saved state | Complete: normal/fast user report, observed full process exit/restart, matching build/settings, and user-confirmed A name/location after reload. Brief black transition and unmeasured world-load time retained as limits |
| Cycle 2: action, full exit/relaunch, saved state | Complete: “Restore point saved” reported, normal exit and new process observed, build/settings/older saves checked, and matching A name/location confirmed after reload |
| Cycle 3: action, full exit/relaunch, saved state | Complete: user reported matching action/save behavior, game closed and matching build/settings checked, new process launched, and matching A name/location reported after reload |
| Distinguishable B saved and verified after reload | Complete on the observed retest: accepted B name, ship action/save popup, updated 20:47 save timestamps, and B in Options after full exit/Steam relaunch/ordinary slot load. Earlier unsuccessful B probes retained |
| A restored and verified in the same slot/load operation | Complete: four files matched A before launch; older saves unchanged; ordinary slot load showed A with the original save times, and the user confirmed the same ship/location |
| Original settings restored and existing saves checked | Complete: Multiplayer Enabled persisted; Cross-Save fields unchanged; original Steam Cloud on and final Up to date reported by user; synchronized A load verified. Older saves unchanged; additional native title entry retained |
| Final game fingerprint | Complete: Steam build 25442159, executable version 179666 and SHA-256 match preflight; inspected mod directories remain absent |

Before any replacement, identify the new slot by before/after file observations, declare every file in its checkpoint, and specify the sync sequence from observed settings. Keep the existing four save-related files outside that set. A/B labels must be shown to survive native save/reload before they are used as recovery evidence.

The selected sequence is: close NMS normally and wait for Steam sync; turn off Steam Cloud for NMS only; take an original profile/settings recovery snapshot; create a new disposable slot and disable native multiplayer through its normal setting; identify and back up the exact checkpoint A file set; complete E-01a's cycles and B-to-A recovery with lab settings held fixed; restore the original multiplayer and Steam Cloud settings; then verify A again after a normal synchronized launch. Keep the original Cross-Save values unchanged. The broader original backup is a recovery copy, not permission to replace unrelated saves. Cloud conflicts and shared-metadata dependencies require observation and a specific resolution before replacement.

## Result and limits

Failure accounting: no functional failure was reported in the **three counted native cycles**. The earlier B preparation sequence produced **two unsuccessful marker checks** (ordinary load and native Restore Point both showed A); that sequence remains uncertain. The directly observed rename/save retest verified B after restart, the **single file-restoration attempt** passed, and both the restored-A load and final Cloud-on load passed. This demonstrates the corrected procedure without diagnosing the earlier behavior as a game defect.

Preflight and local copies succeeded. **Three of three native action/reload cycles matched**, with no functional failure reported in those cycles. A directly observed retest established **B surviving a full restart and ordinary slot load**, followed by **successful file restoration and native loading of A**; the earlier unsuccessful B probes and uncertain sequence remain in the record. All four restored files matched A before launch, and older saves were preserved. **E-01a / T-03.1 / R-011 passed within this scope.** Original Multiplayer and Steam Cloud preferences were restored, A loaded after synchronization, the final normal exit/sync was confirmed, and final build/settings/older-save checks passed. The labelled A slot and ignored backups are retained; NMS and the observer are closed. Process-start timings are measured; menu/world load time is not. The E entry prompt was captured during B setup, while the exit-key prompt and the original cycles' keys were not. The brief black-screen observation remains without an inferred cause. No frame-performance result is claimed.

## Documentation history

These dated records were moved from STEPS; their counts and limitations describe the checks at that time.

| Date | Record | Checks and limits |
| --- | --- | --- |
| 2026-09-21 | T-03.1 / R-011 preparation | User choices, initial installation checks, and detailed verification record are in the [task plan](README.md#preparation-record--2026-09-21). E-01a requirements and baseline sources prepared. Whitespace and document structure passed; one pre-existing README link has no target. No game experiment or application test occurred |
| 2026-09-21 | T-03.1 plan relocation | Moved detailed preparation into the task plan; execution authorized for this step only. `git diff --check` and `git diff --no-index --check -- NUL docs/plans/T-03.1.md` found no whitespace errors. Six affected Markdown files, 31 relative links, five anchors, and balanced fences checked. Other queued tasks, E-01b onward, and ROADMAP unchanged. Documentation only; no game actions or application tests in this relocation |
| 2026-09-21 | T-03.1 / R-011 / E-01a complete | Three native action/reload cycles matched; observed B-to-A recovery and final synchronized A load passed. Original Multiplayer/Cloud preferences restored; older saves and exact build/hash preserved. Earlier failed B probes, captures, local commands/results and remaining measurement limits are in the execution record. No other step was executed |
| 2026-09-21 | T-03.1 verification and handoff | Tracked/new-document whitespace, 13 Markdown files, 61 relative links, six anchors, stable IDs, five detailed tasks and balanced fences checked; all 68 referenced local artifact filenames found. The pre-existing missing README target is the only link failure. Raw saves/captures/helpers remain ignored. Other queued task bodies, E-01b onward and ROADMAP unchanged. No application tests exist or were run; changes remain local and uncommitted |

The subsequent documentation cleanup moved this task's plan, procedure, sources, and evidence into one folder. It changed no experiment criteria, observations, game files, or other task requirements. The obsolete README link to an unavailable external Playbook was removed from navigation; that document was not read or copied.

Cleanup verification: `git diff --check` and new-file `git diff --no-index --check -- NUL <path>` checks passed. All 15 Markdown files, 80 relative links and 13 anchors validated; fences, 140 roadmap IDs, five queued tasks and ten experiment IDs remained valid. Comparison with the pre-cleanup documents preserved all seven E-01a procedure steps, its acceptance paragraph, the full original run record, and all six baseline source URLs. E-01b onward and the architecture/roadmap were unchanged. T-04.1 changed only where it places command documentation; the other queued task bodies were unchanged. Independent review corrections addressed evidence-format navigation, historical command preservation, and documentation ownership. About 1,800 words were removed from the main documents. No game actions, external-source refresh, or application tests were performed during this cleanup; changes remain uncommitted. Automatic approval review blocked optional removal of the two old empty folders with the reason "blocked by policy"; their files were already relocated, and the empty directories were left in place.
