# T-04.4 — Persistence execution record

Started 2026-09-23, America/New_York. [Procedure](README.md); [sources](sources.md); [STEPS](../../STEPS.md) owns delivery status.

## Result and limits

**T-04.4 / E-10 persistence extension / M-03 passed for the declared member-owned chat progression.** One owned Windows/Steam client, across two attached game processes, produced six user-confirmed native replies: **0 → 1 → 1**, then after closing the game and restarting the empty service, **1 → 2 → 2**. Member/interaction IDs, state and replay outcomes survived. Exact replay added no progress; an altered replay was rejected. Disable, normal native restart, settings restoration and preservation of older saves passed.

Software verification passed **44 service checks and 74 adapter tests**. Independent review of transactions, replay expiration, identity separation and maintenance refusal found no actionable blocker. No game crash, expired result or dropped adapter diagnostic was observed during this task. The development failures are retained below.

The T-04 parent remains incomplete. Progress counts cooperative chat interactions, not verified physical gameplay or currency. Automatic native presentation, explicit engine lifecycle callbacks, two real clients, public identity/entitlement and stronger gameplay authority remain open. Storage is one local process; events currently grow without archival. The maintenance command creates/validates initial schema v1; historical schema upgrades remain future work. Transaction-boundary exception tests and post-acknowledgement process termination do not establish every interruption point or hardware power-loss durability. Changes remain local and uncommitted; hosted CI has not run for this task.

## Software checks

Toolchain: Windows x64, .NET SDK `10.0.302`, .NET/ASP.NET Core runtime `10.0.10`, Python 3.13. The isolated native adapter environment remains CPython 3.13.14 with the existing 19-package lock; `pip check` passed. `dotnet restore CommunityPlatform.slnx --locked-mode` succeeded. NuGet reported no vulnerable packages from its current configured advisory source for the service, including transitives; this is not a general security audit.

`dotnet run --project tests/Community.Tests --configuration Release` passed **44 integration checks**, including actual child HTTP processes, graceful and abrupt restart, stable member identity across session/key changes, concurrent duplicates/conflicts, more than 256 actions, bounded history pages, 24-hour replay pruning, offline backup/restore, exclusive ownership, corrupt/empty/foreign/newer-schema refusal and redaction. Evidence: ignored `local/t04-4/tests-20260923-231214-259-1384414ff74e496f98385cc3e2900c11`. All suite children stopped.

Failure-before-commit, lost-response-after-commit and initial-migration rollback cases inject internal exceptions at transaction boundaries. Retention cases inject server time and inspect actual SQLite rows. These are software fault simulations; the post-acknowledgement kill is an actual child-process termination. No power-loss/storage-hardware or concurrent multi-process writer guarantee is inferred. Initial schema creation/validation is exercised; there is no historical durable schema to upgrade. Backup restoration intentionally recovers its checkpoint and cannot recover later acknowledgements.

The final command `py -3.13 -W error::ResourceWarning -m unittest discover -s tests/Community.AdapterLab.Tests -q` passed **74 tests** in 10.884 seconds without skips or warnings (output `local/t04-4/adapter-tests.txt`), including three real Python-to-C# process runs, a new-client read-only snapshot after empty-server restart, exact persisted replay, progress beyond revision 256, strict state/member validation, cancellation and callback correlation. These use synthetic native callbacks, so they establish service/adapter behavior only. Process artifacts are in ignored `local/t04-4/tests/`.

During integration, a build was attempted before the test agent finished its file; the resulting missing test helper errors were resolved by finishing that work. A subsequent test compile needed its async hash comparison materialized before span comparison. Concurrent build/test processes caused one transient output-file copy retry. An initial runtime suite passed 40 checks before an outdated startup-error string assertion failed; the assertion was updated to the new maintenance diagnostic and the full suite above passed. These were development failures, not game results.

New dependencies are Microsoft.Data.Sqlite 10.0.12 and SQLitePCLRaw 2.1.12; exact package hashes are in the lock files and source/license review in [sources](sources.md). Original license/notices are copied with build outputs. Repository checks passed at this preparation handoff: 29 Markdown files, 265 relative links, 72 anchors, all 140 unchanged roadmap IDs, balanced fences, tracked whitespace and 14 new files compared using `git diff --no-index --check`. Result: `local/t04-4/repository-checks.json`. Hosted CI has not run for these local changes.

Read-only executable preflight still matched SHA-256 `B7913F268DFC62386B6B68F524BFC8ADE4A44A9F4FBAD39085B7BF51BE3680CB`; both selected chat signatures remained unique eligible entries. This is attachment eligibility, not proof of the new game operation.

## Attended game evidence

The user confirmed NMS closed, Steam synchronization finished and NMS-only Steam Cloud off. `local/t04-4/prepare-lab.ps1` copied and hash-verified eight files: four disposable save files and four settings references. The private receipt also preserves old-save hashes. No source save was overwritten. Initial settings were Multiplayer true, CrossSaves false and CrossSavesAutoUploads true; file flags do not establish account/UI behavior.

Private server/principal-only configs are under `local/t04-4`, with message `Community progress saved by your server.` Server A process `33416` passed health at protocol version 2; its fresh state is progress/revision 0. Steam's manifest reports build `25442159`. Steam launch was requested through `steam://rungameid/275850`, recorded in `local/t04-4/launch.json`. First attachment and visible results are recorded below.

### First attachment

The user confirmed checkpoint A loaded and Multiplayer disabled. File inspection corroborated Multiplayer false and CrossSaves false; CrossSavesAutoUploads remained true. NMS process `15084` started at `2026-09-24T02:48:54.445Z` (22:48 local on September 23). The executable fingerprint remained unchanged after Steam launch. Environment/config/server-binary hashes are in `local/t04-4/run-001-environment.json`.

The guarded launcher process `21960` attached with `--pid 15084 --run-dir local/t04-4/run-001 --service-config local/t04-4/adapter.json`, using the isolated native Python environment. Executable/live-signature/settings/dependency gates passed. The launcher recorded source hashes before injection; `pymhf-20260923T225152.log` reports one mod/two hooks loaded in 0.790 seconds, followed by the developer listener. This is loader time, not frame-time overhead. The game remained responsive. The initial progress check succeeded as recorded below.

### Initial read, acknowledged action and read-only confirmation

The user confirmed all three native-chat results: **0 → 1 → 1**. Adapter logs correlate the reads and mutation with server state; callbacks ran on observed native thread `29180` and HTTP completion on worker `37980`. Thread IDs describe this run only. No expired result, dropped diagnostic or game failure is recorded in this segment.

| Operation | Queued UTC, 2026-09-24 | Request ID | Progress / revision | Native retrieval after queue |
| --- | --- | --- | --- | --- |
| Read `/community state` | 02:53:01.310 | `4f932e92-4eb3-4539-964f-2f62b5670b7a` | 0 / 0 | 4.750 s |
| Mutate `/community` | 02:53:15.176 | `ad9de2c8-bf5d-4993-b8b9-8c780ebea0b9` | 1 / 1 | 3.183 s |
| Read `/community state` | 02:53:25.693 | `06cfbac1-8230-4aac-bff7-5e4c7edfb88e` | 1 / 1 | 3.483 s |

Member `14c4e4aa-1734-45ac-9893-390ada7d6e1d` and interaction `23786e4a-2a50-4b8b-986f-5e962969a813` match all responses. The server logged HTTP 200 for the mutation request, which persisted event `fc89ed12-d9cd-4982-8503-bab69872556a`. Read-only inspection captured its original request and response into ignored `local/t04-4/acknowledged-game-request.json`; `initial-round-trips.json` records timings. A separate diagnostic adapter-scoped session was created solely to test token invalidation after restart; its bearer token stays in an ignored private file and is not a game-client credential capture.

### Empty-server restart, backup and replay

The user submitted `/community leave` and exited normally. The adapter recorded `community_left` at `2026-09-24T02:54:11.044Z`, generation 1; subsequent inspection found no NMS process. A service snapshot still showed progress/revision 1. Server A accepted the authenticated stop and exited normally. The offline `--backup` command produced verified `local/t04-4/backups/progress-1.sqlite3`; its hash and closed-game checkpoint are in `empty-server-checkpoint.json`.

Server B process `33456` started on the same endpoint/configuration/database with no game running. At `2026-09-24T02:55:27.550Z`, a diagnostic check proved: the pre-restart diagnostic token was rejected with `invalid_session`; a fresh adapter-scoped session recovered the exact acknowledged state; replaying the original game request returned its exact original response/event; the durable history still contained one event; changing the request body while reusing its ID returned `request_id_conflict`; progress remained 1. The diagnostic session was closed. Detailed sanitized result: `local/t04-4/restart-replay-result.json`. This replay is a service diagnostic of the real game request, not a second game action.

The same member, interaction and event UUIDs survived. No timer or passive change ran while empty. Steam relaunch for `run-002` is recorded in `launch-002.json`; visible recovery after reattachment is recorded below.

### Second attachment

The user confirmed checkpoint A reloaded and Multiplayer disabled. Settings again corroborated Multiplayer false/CrossSaves false. Server B remained ready. The guarded launcher `24672` attached to the new NMS process `26280` into `local/t04-4/run-002`; executable, live signatures and dependency gates passed. One mod/two hooks loaded, and the new `probe_loaded` event was recorded. The game remained responsive. Adapter source manifests match exactly across both attachments. The visible recovery and next action passed as recorded below.

### Visible recovery and continued progress

The user confirmed native-chat values **1 → 2 → 2**, in order: read persisted state, perform one new action, read again. The first read recovered the earlier acknowledged value before any new mutation. Both runs have the same member and interaction UUIDs.

| Operation | Queued UTC, 2026-09-24 | Request ID | Progress / revision | Native retrieval after queue |
| --- | --- | --- | --- | --- |
| Recovered read | 03:03:05.510 | `3e54bc87-5ee0-4897-988a-a03c159b6f50` | 1 / 1 | 4.874 s |
| New action | 03:03:15.944 | `239565d9-1f85-4a0a-92cf-493248648128` | 2 / 2 | 2.885 s |
| Read-only confirmation | 03:03:24.818 | `67820ea4-7a6c-4295-9bee-3a52f8522014` | 2 / 2 | 2.800 s |

Native callbacks were observed on thread `31564`; service completion on worker `17448`. `recovered-round-trips.json` records correlation. A fresh diagnostic snapshot/history check confirmed progress/revision 2 and exactly two committed events, retaining the original event and one new event for the second game action. `final-progress.json` holds that private evidence. Six user-visible replies across two actual game processes establish the selected persistence behavior. No native save value is used to recover the member's progression. Disable and normal native restoration passed as recorded below.

### Disable and normal recovery preparation

The user confirmed `/community off` and normal game exit. The adapter recorded `community_left` with `disabled:true` and `disabled` at `2026-09-24T03:04:06.734Z`. NMS was absent before cleanup. Server B accepted shutdown and exited; offline backup `local/t04-4/backups/progress-2.sqlite3` was verified. Both lab listeners, 18181 and 6770, were absent. A normal Steam launch was requested without attachment; `launch-recovery.json` records it and the final database backup hash. User verification of native behavior and restored settings/sync passed as recorded below.

### Final restoration and handoff

The user confirmed checkpoint A played normally without the adapter, `/community` returned ordinary native behavior, Multiplayer was restored to Enabled, NMS exited normally, and NMS-only Steam Cloud was re-enabled with synchronization complete. The normal unmodified recovery process was `38908`; no adapter was attached to it.

`local/t04-4/check-restoration.ps1` recorded at `2026-09-24T03:07:10.706Z` (23:07 local on September 23): NMS closed; lab listeners 18181/6770 absent; all four older save files unchanged; all four settings reference files byte-for-byte unchanged; Multiplayer true, CrossSaves false and CrossSavesAutoUploads true; executable unchanged; no NMS Application Error event 1000 since the first launch; no mod directories added. `final-restoration.json` holds the result. Steam Cloud/sync confirmation is the user's observation, not inferred from those file flags. No save restoration was required, and ordinary disposable-save changes were retained.

Both server processes stopped. The ignored final database and verified progress-1/progress-2 backups remain available for subsequent work. Raw game/save material, credentials, private sessions, database files and runtime captures stay outside Git. The next planned task is [T-04.5](../T-04.5/README.md), investigating automatic native presentation and one in-world interaction before multiplayer gameplay builds on the chat bridge.

Final repository verification passed after the completed game record and next-task plan: **30 Markdown files, 282 relative links, 77 anchors, 140 unchanged roadmap IDs, balanced fences, tracked whitespace, and 15 new files included in Git comparisons**. The final result overwrites `local/t04-4/repository-checks.json`. No application code changed after the passing service/adapter suites and game runs; no redundant test rerun was needed. The CI workflow reuses the locked restore/service test and Python test commands, but no push or hosted execution occurred during T-04.4.

## Post-review changes (2026-09-24)

Made after the attended game runs above, so those runs used the earlier adapter check:

- The adapter no longer requires a probe to add exactly one progress. It verifies only that revision advances by one and progress does not decrease, leaving the amount to the server. The server rule is unchanged (+1), so observable game behavior is unchanged.
- Restore now also refuses a destination holding a leftover `-journal`, `-wal` or `-shm` file.

Rerun after these changes: **44 service checks and 75 adapter tests passed** (one new adapter test for the progress rule; the restore check gained a leftover-journal case). No new game run was performed.
