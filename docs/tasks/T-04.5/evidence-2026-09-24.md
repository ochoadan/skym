# T-04.5 — Evidence, 2026-09-24

This record preserves the first observation pass. The subsequent [receiver validation, automatic interaction and recovery record](evidence-automatic-2026-09-24.md#recovery-result-and-limits) contains the completion evidence.

## Result and limits

First observation pass and recovery complete; T-04.5 remains in progress. The observation-only cockpit/update/FSM experiment produced four user-confirmed ship-entry callbacks, including one after menu/reload; disable stopped observation and the game exited normally. All six selected signatures passed file/live checks, and 87 adapter tests passed. Unmodified recovery and restored control settings are verified; game-added catalog-history entries were preserved as detailed below. Automatic presentation has not been implemented or demonstrated; a [fresh receiver/state candidate](sources.md#local-static-receiver-investigation) now needs read-only native validation. The selected operation and attended procedure are in the [task record](README.md#selected-first-experiment-observe-cockpit-entry-and-scheduling).

## Read-only checks

NMS was closed during preparation. Steam executable version `179666`, 88,480,328 bytes, SHA-256 `B7913F268DFC62386B6B68F524BFC8ADE4A44A9F4FBAD39085B7BF51BE3680CB`; same fingerprint as the recorded Steam build `25442159`. No game build allowlist was changed. All new diagnostics and captured metadata stay under ignored `local/t04-5`.

`candidate-preflight.json` records eleven candidate patterns, each with one match. `cTkFSMState.StateChange` (RVA 45978400) and `cGcSimpleInteractionComponent.DoAction` (17174448) failed the complete runtime-function-entry-prefix requirement and were excluded. No exception was added to the gate.

`selected-preflight.json` records exit 0 for:

| Hook | RVA | Matches |
| --- | --- | --- |
| Parse text | 11616864 | 1 |
| Say | 11621888 | 1 |
| Application update | 2979072 | 1 |
| Parent FSM transition | 45978192 | 1 |
| Player update | 21236944 | 1 |
| Cockpit entry | 21468304 | 1 |

This permits an experiment and does not prove callback semantics, lifecycle ownership, presentation safety or frame-time overhead.

## Implementation and verification

The launcher’s opt-in `--observe-world` flag chooses the six-hook profile before injection and requires a new run directory below `local/t04-5`. Supplying a service configuration is rejected by both launcher and injected module. The native callback layer preserves the existing chat path. The new observer uses only scalar counters and per-thread depth; it does not retain native arguments, invoke game functions or send service requests.

`py -3.13 -W error::ResourceWarning -m unittest discover -s tests/Community.AdapterLab.Tests -v` passed **87 tests** in 10.116 seconds. `local/t04-5/adapter-tests.txt` holds the output. Coverage adds extended-profile refusal without changing the chat profile, empty-profile rejection, nested/thread-local scheduling, bounded diagnostics, contention, disable, emitter failure and detached scalar records. Existing process/service bridge tests also passed. No service code or dependencies changed; the service's standalone suite was not rerun for this observer-only change.

Independent pre-attachment review checked the selected signatures/ABIs against the pinned source, opt-in isolation, both service/observer refusals and disable behavior. No blocking defect was found. The shared detail budget can exhaust during a long session; summary counts then cannot supply missing per-callback thread/nesting evidence.

## Attended run preparation

The user confirmed availability for the attended game test and asked whether setup could be automated. Steam's library UI visibly reported NMS Cloud status **Up to date**; the per-game Cloud toggle was on, then switched off through that UI and visually checked. `steam-focused.png`, `steam-properties-before.png` and `steam-properties-cloud-off.png` retain private captures. No Steam configuration file was edited. An earlier capture was obscured by the browser and was not used as Steam-state evidence.

`prepare-lab.ps1` verified fresh copies of the four disposable-save files and four settings references while NMS was closed; `snapshot-before-game/receipt-private.json` records their hashes and the four older-save reference hashes. Multiplayer was true, CrossSaves false, and CrossSavesAutoUploads true before launch. A normal Steam `-applaunch 275850` launch created NMS PID 7552; `launch.json` records the request time. The menu showed executable version 179666.

The user then asked to stop UI automation because it was slow and return to manual prompts. UI automation stopped at the main menu. The user subsequently confirmed checkpoint A loaded beside the landed ship with Multiplayer off. The observed process at attachment was PID **22820**, started 20:38:05 local, replacing the earlier unmodified PID 7552; the reason for that process change was not recorded. No NMS Application Error event 1000 was found during the preparation interval.

## Run 001: observed input and scheduling

Guarded launch command, using the isolated Python environment:

```powershell
.\local\t04-2\venv-native\Scripts\python.exe -u src/Community.AdapterLab/launch.py --pid 22820 --run-dir local/t04-5/run-001 --observe-world
```

`attach-001.json`, launcher stdout/stderr, `run-001/preflight.json`, `source-hashes.json`, `events.jsonl` and the pyMHF log retain private evidence. Both guards passed. The loader reported one mod and six hooks loaded in 1.583 seconds; its console listened on loopback 6770. This was one owned client, Multiplayer/Cross-Save off, with no community service connection. Exact source hashes are in the run receipt.

The user confirmed the visible local `/community` message and **three normal landed-ship entry/exit cycles**. Callback records, UTC on **2026-09-25** (September 24 local):

| Observation | UTC | Thread | Within application update on that thread |
| --- | --- | --- | --- |
| First application update | 00:40:52.010 | 17608 | Yes |
| First player update | 00:42:29.977 | 16684 | **No** |
| Local command's native Say / written response | 00:42:34.241 | 17608 | Yes |
| Cockpit entry 1 | 00:42:44.874 | 17608 | Yes |
| Cockpit entry 2 | 00:42:54.754 | 17608 | Yes |
| Cockpit entry 3 | 00:43:08.504 | 17608 | Yes |

The three typed cockpit callbacks match the three user-confirmed deliberate entries. No native pointer or chat text is in the records. This establishes the narrow input observation; it does not prove unique semantics across loading or authorize rewards. The different player-update thread **rules out treating that hook as a general main-thread presentation callback**. Only its first detail is logged, so this does not enumerate every thread on which it can run.

### Menu/reload, disable and normal exit

The user confirmed return to mode selection, reloading checkpoint A, one normal ship entry/exit, the visible `/community off` message, one further normal entry/exit after disabling, and normal game exit.

Three parent-FSM transitions were observed at **00:46:25.892**, **00:46:29.932**, and **00:46:43.513** UTC, all on thread 17608 inside application update. They correlate with the user's menu/reload sequence; the current observer cannot assign individual state names or prove the FSM's ownership. The next cockpit callback at **00:46:46.874** was count 4, also on thread 17608 inside application update. No additional cockpit callback was seen before that deliberate post-load entry in this sequence; broader spawn/load behavior remains untested.

At **00:46:57.491**, the off command produced the native response and `world_disabled`. Final counts were 13,007 application updates, 3,252 player updates, **4 cockpit entries**, **3 transitions**, 2 native Say callbacks and 11 detail records, with both drop counters zero. No events followed disable, despite the user-confirmed fifth entry/exit. `observation-summary.json` records event correlation and the original source hash receipt. This is behavior disablement, not live detour removal.

At **00:47:29.029**, `post-observer.json` recorded NMS closed, listeners 18181/6770 absent, and no NMS Application Error event 1000 since initial launch. The launcher logged its normal cleanup. Absence of that Windows event is supplementary evidence, not a universal crash detector. The user supplies the normal-exit observation.

Principal run-source SHA-256 values:

| Source | SHA-256 |
| --- | --- |
| `probe.py` | `d16747fcf2fee25e6c61092a14cfed65bd27380964260a8fcc867b1db5615daf` |
| `world_observation.py` | `98463d1bdda613f54e639a668af6d30da5e119ca6f50079dabef5aecacbde684` |
| `launch.py` | `4c0bb3d2ffbfa5945337e6021ebb78c08f38e80cd9a94aef78937a93e292ca82` |
| `runtime_guard.py` | `58822f6aea3658743ff317e7e2dfd79008ccfc4cc96423c6d252457f0e44afe2` |
| `preflight.py` | `27065a1c2c76e0848f03b8674750d0e8ffbc9644c913b3f4367c5e065afbf4e7` |

The adapter remains observation-only; no automatic native call is enabled. No frame-time comparison, ten-process restart matrix, travel coverage, full E-03, gameplay authority or two-player behavior was tested.

## Final restoration and handoff

The user confirmed that a normal Steam launch without attachment played checkpoint A correctly, `/community` produced ordinary native behavior, Multiplayer was restored to Enabled, the game exited normally, and NMS-only Steam Cloud was re-enabled with synchronization complete. The unmodified recovery process was PID **24572**; `recovery-process.json` records its observation without an adapter listener. Cloud and visible game behavior are user observations, not inferred from local settings flags.

At **2026-09-25T01:07:37.645Z** (21:07 local on September 24), `local/t04-5/check-restoration.ps1` recorded in `final-restoration.json`:

- NMS closed and lab listeners 18181/6770 absent.
- All four older-save hashes unchanged; executable hash unchanged; no mod directories added.
- Multiplayer true, CrossSaves false and CrossSavesAutoUploads true, matching the initial control settings.
- Three of four settings files byte-for-byte unchanged. `GCUSERSETTINGSDATA.MXML` differed only in its `SeenProducts` and `SeenTechnologies` subtrees, as established by a complete top-level XML comparison.
- No NMS Application Error event 1000 since initial launch, supplementing the user's normal-exit report.

`settings-difference-summary.json` records the user-settings difference: seen products grew from 135 to 1,480 (1,345 added, none removed); seen technologies grew from 25 to 138 (113 added, none removed). All other settings subtrees were identical. The specific game activity responsible for those additions was not measured. These are game-managed catalog-history entries, not a demonstrated adapter write; the observer does not write settings. They were retained rather than overwriting normal game changes to force matching hashes. No save or settings backup was restored; ordinary disposable-save changes were also retained. Raw XML differences and backups remain ignored private evidence.

`source-unchanged.json` confirms all nine adapter Python sources still match the attached run's receipt. No application code changed after the 87 passing tests and native run, so this final restoration/documentation update did not repeat application tests. Repository verification checked 32 Markdown files, 307 relative links, 89 anchors, balanced fences, 140 unchanged roadmap IDs, tracked whitespace and all four new files through Git comparisons; `repository-checks.json` records the result.

Next: implement the separately selected read-only receiver/state validation described in the source review, then use its native observations to decide whether to enable an automatic presentation experiment. The working chat/service path is preserved. T-04.5, the parent T-04 work package and the broader E-03 remain incomplete. Changes are local and uncommitted; no push or hosted CI run occurred in this task.
