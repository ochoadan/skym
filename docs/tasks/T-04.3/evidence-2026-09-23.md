# T-04.3 — Service/game connection execution record

Run date: 2026-09-23, America/New_York. Experiment: E-10 initial loop. Codex owns code/process/log observations; the user owns in-game actions and visible response observations. [Procedure and scope](README.md); [STEPS](../../STEPS.md) owns status.

## Result and limits

**T-04.3 / E-10 initial loop / M-02 passed for the declared explicit chat-polling operation.** One owned NMS client produced six timely, user-confirmed server replies across rule change, service restart and menu/reload. Outage feedback, leave/rejoin, disable, normal native restart and final settings/save recovery passed. One earlier reply committed at the service but expired before manual retrieval; it is not counted as a visible rule success. There was one successful attachment and no observed game crash during this task.

The implementation has no persistence or automatic pushed presentation, and does not complete every broader R-021–R-030 acceptance item. The detailed remaining scope is below. T-04.4 is next; the T-04 parent remains incomplete.

## Software checks

The service suite ran with .NET SDK `10.0.302` and installed .NET/ASP.NET Core `10.0.10` runtimes: `dotnet run --project tests/Community.Tests --configuration Release` passed 33 integration checks, including 11 new compatibility/scope/probe/replay cases. Logs are retained under ignored `local/t04-1/tests-20260923-222055-658-0f0ab35bfe0841989288ccc2c3ce2c74`. All suite child processes stopped.

The final adapter command, `py -3.13 -W error::ResourceWarning -m unittest discover -s tests/Community.AdapterLab.Tests -q`, passed **63 tests** in 7.985 seconds without skips or warnings. Output is `local/t04-3/adapter-tests.txt`. This includes 28 earlier tests, 22 strict client/HTTP tests, 11 worker/callback lifecycle tests, and two actual Python-to-C# process tests. The latter exercise five actions, outage/reconnect, changed configuration/restart, two endpoints/communities, correlation and credential redaction. Their native callbacks are synthetic; they do not establish game behavior. All test servers stopped.

Review/tests corrected three issues before attachment: independently validate nested event state instead of relying on Python numeric equality; close partially read HTTP responses; and do not disable command handling when an off command produces no native system reply. Independent review also identified that `Queue.put_nowait` still takes a blocking internal mutex. Worker/log queues now use CPython `SimpleQueue`, with capacity enforced under explicit nonblocking producer gates. No network or worker join occurs under a callback-shared lock. The full suite above ran after these changes.

## Preparation and loaded version

The user confirmed NMS closed, completed Steam synchronization, and NMS-only Steam Cloud off. `local/t04-3/prepare-lab.ps1` copied and hash-verified the four disposable save files and four settings references into `snapshot-before-game/`; the private receipt also records the four older saves for preservation checks. No source save was replaced. Initial Multiplayer was true; CrossSaves was false and CrossSavesAutoUploads true.

The private task server configuration and principal-only adapter configuration were generated under `local/t04-3`. The first selected message is `Server rule A: welcome to the community.` No credentials are included here. Server A process `38832` passed readiness and the diagnostic malformed request returned `invalid_json` before attachment. Server output is `server-a.stdout.log`; bootstrap and operator keys were not printed.

Steam launch created NMS process `34572` at **18:24:49 local**. The user confirmed loading `T-03.1 checkpoint A` and disabling Multiplayer; they had not located the Cross-Save UI. File inspection corroborated Multiplayer false and CrossSaves false; the auto-upload field remained true. No account/UI behavior is inferred from these flags. Steam manifest still reported build `25442159`.

Read-only and live preflight matched executable SHA-256 `B7913F268DFC62386B6B68F524BFC8ADE4A44A9F4FBAD39085B7BF51BE3680CB`; both chat signatures remained unique eligible function starts. Existing Windows/Steam installation, hardware, display and disposable-save scope are those recorded in [T-04.2](../T-04.2/evidence-2026-09-23.md#claim-and-environment). Runtime remained isolated official CPython 3.13.14 with the exact 19-package lock; `pip check` passed. One owned client, loopback transport, no native multiplayer participants.

Attachment command:

```powershell
.\local\t04-2\venv-native\Scripts\python.exe -u src/Community.AdapterLab/launch.py --pid 34572 --run-dir local/t04-3/run-001 --service-config local/t04-3/adapter.json
```

The launcher recorded all adapter Python source hashes in `run-001/source-hashes.json` before injection. The corrected injector verified target DLLs/export and the mod repeated the live gate. `pymhf-20260923T182707.log` reports one mod/two hooks loaded in 0.534 seconds; this is loader time, not frame-time overhead. `probe_loaded` occurred at **22:27:07.931 UTC**. NMS remained responsive. No code was reloaded while attached.

## Initial request and delayed retrieval

At **22:27:45.000 UTC**, native chat sequence 1 queued request `483b2154-25f9-49ff-a17d-5cdff834e919`; the server committed revision 1 and the worker received the reply. The result command arrived **33.75 seconds** after submission, beyond the declared 20-second lifetime. The user observed the expiry message, then deliberately tried again. This is a committed service interaction with an expired presentation, not a successful visible rule reply. It demonstrates timeout feedback and also exposes a manual-entry usability limitation.

The retry, request `30651de6-9ae3-4bdc-ba69-ebe3002f95fe`, queued at **22:28:25.050 UTC**, received revision 2, and was presented through native chat sequence 4 about **3.18 seconds** later. The user reported seeing **rule A**. The server request ID matches its successful command log; queued/presentation callbacks used observed native thread `13096`, while HTTP completion used worker thread `25708`. Thread IDs are observations, not a general engine-thread contract.

## Repetition, service outage and changed rule

The user's repetition report says rule A worked four times, ending at revision 5 because the first expired display had still advanced server state. Logs establish **four timely rule A replies total**, revisions 2–5, not four additional replies after revision 2. The user also reported an empty final result; there is no separate extra result callback in this log segment, so it is not counted as another observed operation. Synthetic tests separately verify one-time result consumption.

| Server / revision | Request ID | Queued UTC | Native result retrieval after submission |
| --- | --- | --- | --- |
| A / 2 | `30651de6-9ae3-4bdc-ba69-ebe3002f95fe` | 22:28:25.050 | 3.183 s |
| A / 3 | `9afe9eba-9dde-4d88-81aa-23cde913501f` | 22:29:17.450 | 4.033 s |
| A / 4 | `962f49cb-9e71-4b90-83ed-396a80fe92d9` | 22:29:24.084 | 3.233 s |
| A / 5 | `ec93f7de-4661-4932-b81f-4d5205bb56e7` | 22:29:29.767 | 3.300 s |
| B / 1 | `274f9afe-9969-4864-b95a-de240fa55df4` | 22:33:39.176 | 4.033 s |

These intervals include the user's second chat submission. They are not network-latency or frame-time measurements. Each successful server command has a matching request ID in the server log and a worker reply followed by a fresh native presentation callback.

The authenticated diagnostic stop command ended server A at **22:30:30.325 UTC**; its process and listener were absent. At **22:32:00.327 UTC**, `/community` queued `d47c8cab-73ab-4322-a536-9cb70cdea924`. The worker recorded `unavailable` approximately 2.05 seconds later; native retrieval occurred after 9.133 seconds. The user confirmed the unavailable message and a responsive game. This is an intentional outage result, not a successful action.

With the service stopped, only `completionMessage` was changed to **`Server rule B: the community configuration changed.`** Server B started as process `41100` on the same `127.0.0.1:18181` endpoint, with the same community and lab keys. The same attached NMS process submitted the B/1 request above. The obsolete bearer session was rejected, the worker opened a new scoped session before mutation, and the user confirmed the exact rule B response. That gives the fifth timely, user-confirmed round trip. Revision reset is expected memory-only behavior, not persistence.

## Leave, menu/reload and disable

The user reported `/community leave`, return to title, reload of checkpoint A, another successful rule B reply, disable, restored native unknown-command behavior, Multiplayer on and normal exit, without freeze or crash. Logs show generation 1 after leave and a new session. Request `047ebfa2-a073-4dfe-969f-678f568f0b92` queued at **22:34:55.620 UTC**, received B/revision 2, and was retrieved 2.900 seconds later. This is the **sixth** timely, user-confirmed rule reply. Menu/loading themselves are user observations, not engine lifecycle events from this adapter.

At **22:35:07.854 UTC**, `/community off` incremented generation to 2 and disabled behavior. The service recorded session closure (204). NMS was subsequently absent. The launcher printed `pyMHF exiting` and returned 1, matching the previously documented upstream self-termination after normal game exit; that exit code alone is not a game crash. Server B stopped gracefully at **22:35:48.506 UTC**.

Final attached event accounting: 18 correlated native commands, each with one requested/written presentation; eight queued service requests; seven service replies (six retrieved within lifetime and one expired display); one intentional unavailable result; two leave/disable transitions; no `no_system_reply`, late-result or queue-drop events. `server_presentation` is emitted for both success and error/expiry text, so its count of eight is not a success count. The raw event SHA-256 is `bde5afbf802fbd03fe2c664a0df55eed9dad789e3ad643931964eaad111ad889`; `local/t04-3/event-summary.json` records per-request timing/classification.

## Loaded source identity

The complete eight-file source manifest remains in `local/t04-3/run-001/source-hashes.json`. Selected operation hashes:

| File | SHA-256 |
| --- | --- |
| `probe.py` | `4e9ff07a7cd32c991ae8914ce7d6f8914efc53f3449e14995a6c623706e7f0bb` |
| `interaction.py` | `ca28c767f6f14cc0503675b273fe4c97ce318cabf8d7d972653dada11e775a39` |
| `bridge.py` | `ea370140b8b0bd89d67d2ee743be9175c89f8f0bd452d3c0b9a7577bb51428c1` |
| `service_client.py` | `a610dfa32266eeb1b090e84c4d0e00e6ac70f3ca4f53acf73b29ce2546f08487` |
| `launch.py` | `5b4c68ae634b792097c38aae881839263fbc7ee22b0e67572795c436d852d14b` |

Raw game/server logs, private configurations, snapshots and source receipts remain ignored. No game binaries, saves, account identifiers or tokens were added to Git. No public deployment, push, interview or message to others occurred.

The loaded source still matched all eight pre-injection hashes after game exit. `service-build.json` records the configuration comparison (only `completionMessage` changed) and own-build artifact hashes: server `D043C99FA6DCBFDD4986F984C5444D459DD431B4CF564CFC1E2AF0B7A7AADBE5`, protocol `721D375DA07E0E603058D174BCB4458B23FDA18E78C9A209ACCED73349C0325D`.

## Final recovery and repository checks

The user confirmed restoring NMS-only Steam Cloud on, completing synchronization, launching normally without the adapter, loading checkpoint A, observing the native unknown-command response, exiting and seeing Steam **Up to date**. These are user observations. No backup was restored over a save; ordinary changes to the disposable slot were retained.

At **22:39:41.090 UTC**, `local/t04-3/final-restoration.json` recorded: NMS closed; neither port 18181 nor 6770 listening; all four older save files unchanged; all four settings files byte-identical to the fresh pre-lab snapshot; executable hash unchanged; both inspected mod directories absent. Multiplayer was true, CrossSaves false and CrossSavesAutoUploads true. No NMS Application event 1000 was found from this task's launch through the check. File/process observations do not independently establish Steam Cloud UI status or general cloud isolation.

Independent implementation and evidence review found no remaining critical issue after the corrections described above. Local software tests passed before attachment and loaded source stayed unchanged afterward. Final repository verification passed: 25 Markdown files, 208 relative links, 59 anchors, balanced fences, 140 unchanged contiguous roadmap IDs, tracked whitespace and Git comparisons of all seven new files. `git check-ignore` confirmed private configs, backup receipts and game logs remain ignored. The report is retained in `local/t04-3/repository-checks.json`. Changes remain local and uncommitted; hosted CI was not run for them.

## Remaining scope

This is an explicitly polled English chat operation on one exact Windows Steam build, one principal and one local server endpoint. There is no automatic result push, explicit playable/menu/travel interface, live unload, prolonged stability/overhead measurement, native multiplayer admission, physical-gameplay authority, persistence or new resource sandbox. The developer execution listener remains upstream lab tooling.

The broader R-021 same-game/two-endpoint check has not run; two endpoint/community selection was verified with synthetic callbacks against real services. R-022 covers exact protocol/adapter/build/operation negotiation for this fixed original operation, not a general resource manifest negotiation. R-015 and full R-020/E-03 remain open. M-02's narrow E-10 acceptance is distinct from completing every row in R-021–R-030.

An initially delayed manual retrieval expired after 20 seconds, despite a successful service commit. Subsequent timely retrievals and explicit failure feedback worked. This usability limit and the required second chat command should remain visible when selecting later automatic-presentation work.
